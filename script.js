    document.addEventListener("DOMContentloaded", {}=>){
         const LikeBtn = document,querySelector(".left.actions.action.firts.child")
      document.addEventListener("DOMContentLoaded", () => {
     // Seleciona o botão de curtida e o íncone de coração
     const LikeBth = document.querySelector(".left-action .action-btn:firts-cild");
     if (!LikeBth) return;

     const LIke5vg - likeBth.query5elector("sv");
     //contador
let texNode = Array.from(likeBthn.childNodes). find{
(node) => node.nodetype === node.TEXT_NODE && node.textContent.trim{} !== ""
      );
      }
                                
